# 0.0.7 实现与发布方案

更新日期：2026-09-17。

1. 完成版本更新至 0.0.7，油猴元数据从 package.json 读取版本。
2. 纳入当前工作区代码复制修复：文档捕获监听，定位相邻代码节点，优先 innerText 保留换行，支持动态按钮。
3. 修复文本选择 CSS 优先级；探针断言有效声明并禁止原无效写法。
4. 保留付费文章提前退出策略，保留原节点、入口事件与正文样式；登录拦截保持现有行为。
5. 完成登录策略、源码 DOM 探针、TypeScript、Vite 与版本校验。新增 Edge 浏览器 DOM 回归及无 Cookie 线上页面检查。
6. 线上文章返回 HTTP 403；该项属于外部阻塞，不视为通过。浏览器注入使用模拟油猴 API，扩展安装及系统剪贴板仍需实际环境验证。
7. 更新当前测试报告与 readme，产物及中间文件放在忽略的 outputs/、dist/。既有 reference/ 不纳入发布提交。
8. 提交源码，推送 master 与 v0.0.7，发布脚本和 SHA256SUMS，并校验远程附件哈希。

新函数保留 Generated by Codex，修改的人工作品函数使用 Modified by Codex，新增代码补充中文注释；不修改 docs/codex/ 之外的权威文档。
