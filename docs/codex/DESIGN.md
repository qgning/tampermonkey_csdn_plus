# 当前版本检查与后续修复方案

更新日期：2026-09-15。当前版本：0.0.5。

## 已完成

- 阅读项目约束和需求。`docs/ARCHITECTURE.md` 与 `docs/DESIGN.md` 不存在；未创建或修改受保护路径。
- 对当前源码、匿名网页 HTML 和网页引用的官方脚本进行兼容性检查。
- 使用 pnpm 8 保持旧锁文件完成构建。
- 添加源码级定向复现工具，并将当前结论和未验证项记录到 `test-report.md`。
- 本次任务范围是检查，未实施业务逻辑修复。

## 建议修复顺序（未实施）

1. 一键复制从按钮定位到最近的 pre，再读取对应 code；排除工具栏、行号和控制按钮，补充不存在代码节点时的处理。
2. 将 CSS 优先级附在属性值上，真实浏览器验证最终 user-select。
3. 普通折叠与 VIP/付费预览分开处理。不能证明全文已存在时保留原始入口，不尝试获取未授权内容。
4. 评估用事件委托和幂等的 DOM 更新处理支持延迟创建的按钮与代码折叠，避免重复绑定以及整块克隆破坏页面交互。
5. 在真实浏览器中按 `../TEST.md` 完成匿名状态交互回归，再更新本版本报告。现有源码探针专门确认旧缺陷；修复时需改为验证期望行为。

## 验证边界

当前只确认选择器在 HTML/官方 JS 中仍存在、复制工具栏关系不兼容，以及源码的确定性问题。浏览器读取超时与 HTTP 521 阻断限制了动态 DOM 和 VIP 验收，不能把静态检查当成完整验收。
